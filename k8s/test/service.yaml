apiVersion: v1
kind: Service
metadata:
  name: mortar
  namespace: mortar-test
  labels:
    app.kubernetes.io/name: mortar
    app.kubernetes.io/part-of: homelab
spec:
  selector:
    app.kubernetes.io/name: mortar
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
